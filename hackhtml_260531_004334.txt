<!DOCTYPE html>
<html lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>⚠️ SECURITY BREACH ⚠️</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; user-select: none; }
        body { background: #000; font-family: 'Courier New', monospace; color: #0f0; padding: 15px; }
        .alert-bar { background: #2a0000; border: 1px solid #f00; border-radius: 12px; padding: 10px; text-align: center; margin-bottom: 20px; animation: pulseRed 0.8s infinite; }
        @keyframes pulseRed { 0% { box-shadow: 0 0 0 0 rgba(255,0,0,0.4); } 100% { box-shadow: 0 0 0 10px rgba(255,0,0,0); } }
        .card { background: #0a0a0a; border: 1px solid #a00; border-radius: 16px; padding: 15px; margin-bottom: 20px; }
        .card-title { color: #ff6666; border-bottom: 1px dashed #f00; padding-bottom: 6px; margin-bottom: 12px; display: flex; justify-content: space-between; }
        .log-terminal { background: #000000aa; padding: 10px; border-radius: 12px; height: 200px; overflow-y: auto; font-size: 11px; border: 1px solid #800; margin-top: 15px; }
        button { background: #1a0000; border: 1px solid red; color: #ff7777; padding: 12px; width: 100%; border-radius: 40px; margin-top: 10px; font-weight: bold; cursor: pointer; }
        .shutdown-btn { background: #220000; border: 2px solid #ff5500; color: #ffaa55; font-size: 18px; }
        .white-flash { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: white; z-index: 9999; opacity: 0; pointer-events: none; transition: opacity 0.05s; }
        .blackout { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: black; z-index: 10000; display: none; justify-content: center; align-items: center; flex-direction: column; color: #aaa; }
    </style>
</head>
<body>
<div class="white-flash" id="whiteFlash"></div>
<div class="blackout" id="blackout"><div style="font-size:40px;">📴</div><div>DEVICE SHUTTING DOWN...</div></div>
<div class="alert-bar"><h2>🔴 CRITICAL SECURITY BREACH 🔴</h2><div>YOUR DEVICE IS UNDER REMOTE CONTROL</div></div>
<div class="card"><div class="card-title">📍 LOCATION & DEVICE</div><div>🌍 کرج، فردیس - خیابان گلستان</div><div id="deviceInfo"></div></div>
<div class="log-terminal" id="hackLog"><p>> SYSTEM HACKED</p></div>
<button id="soundBtn">🔊 ENABLE FULL ATTACK</button>
<button id="shutdownBtn" class="shutdown-btn">📴 REMOTE SHUTDOWN</button>
<script>
    let audioCtx = null, soundEnabled = false, loopInterval = null;
    function vibrate(p) { if (navigator.vibrate) navigator.vibrate(p); }
    function whiteFlash() { let f = document.getElementById('whiteFlash'); f.style.opacity = '0.9'; setTimeout(() => f.style.opacity = '0', 100); }
    function addLog(msg, isDanger = false) {
        let logDiv = document.getElementById('hackLog');
        let p = document.createElement('p');
        p.innerHTML = `> ${msg}`;
        p.style.color = isDanger ? '#ff4444' : '#ffaa66';
        logDiv.appendChild(p);
        logDiv.scrollTop = logDiv.scrollHeight;
        if (isDanger && soundEnabled) { playHarshSound(200, 0.2); vibrate([100]); whiteFlash(); }
    }
    function initAudio() { if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)(); }
    function playHarshSound(freq, duration) {
        if (!soundEnabled || !audioCtx) return;
        let now = audioCtx.currentTime;
        let osc = audioCtx.createOscillator();
        let gain = audioCtx.createGain();
        osc.connect(gain);
        gain.connect(audioCtx.destination);
        osc.frequency.value = freq;
        osc.type = 'sawtooth';
        gain.gain.setValueAtTime(0.3, now);
        gain.gain.exponentialRampToValueAtTime(0.0001, now + duration);
        osc.start();
        osc.stop(now + duration);
    }
    function playBrutalAlarm() { if (!soundEnabled) return; for (let i=0;i<4;i++) { setTimeout(() => playHarshSound(280,0.25), i*350); setTimeout(() => playHarshSound(180,0.25), i*350+180); } }
    function startBackgroundNoise() { if (!soundEnabled) return; if (loopInterval) clearInterval(loopInterval); loopInterval = setInterval(() => playHarshSound(80, 0.1), 5000); }
    let ua = navigator.userAgent;
    let model = "Unknown";
    if (ua.includes("iPhone")) model = "iPhone";
    else if (ua.includes("SM-")) model = "Samsung";
    else if (ua.includes("MI")) model = "Xiaomi";
    document.getElementById('deviceInfo').innerHTML = `📱 ${model} | ${ua.includes("Android") ? "Android" : "iOS"}`;
    document.getElementById('soundBtn').onclick = () => {
        initAudio();
        audioCtx.resume().then(() => {
            soundEnabled = true;
            addLog("[AUDIO] BRUTAL MODE ACTIVATED", true);
            playBrutalAlarm();
            vibrate([500,200,500]);
            whiteFlash();
            startBackgroundNoise();
            setInterval(() => {
                let logs = ["[INFO] Uploading /storage/emulated/0/DCIM/","[WARN] Keylogger data sent","[CRITICAL] Bypassing 2FA","[DATA] Contacts exported","[RANSOMWARE] Encrypting files","[GPS] Sending location","[LOCATION] Target: Karaj, Fardis"];
                let randomLog = logs[Math.floor(Math.random() * logs.length)];
                let isDanger = randomLog.includes("CRITICAL") || randomLog.includes("RANSOMWARE");
                addLog(randomLog, isDanger);
            }, 5000);
        });
    };
    document.getElementById('shutdownBtn').onclick = () => {
        if (soundEnabled) { playHarshSound(40, 1.5); vibrate([1000,500,1000,500,2000]); }
        whiteFlash();
        document.getElementById('blackout').style.display = 'flex';
        if (loopInterval) clearInterval(loopInterval);
        document.querySelectorAll('button').forEach(btn => btn.disabled = true);
    };
</script>
</body>
</html>